# ImportFormat.md

## PPM 文件格式说明

### 1. 概述

PPM（Project Planning Model，项目计划模型）是本作业定义的一种项目调度架构与参数描述文件格式，用于存储任务（Task）、依赖（Dependency）、资源（Resource）及其分配关系。该格式采用纯文本 ASCII 编码，便于人工编写与版本管理。

---

### 2. 文件结构

PPM 文件为多行 ASCII 码文件，仅包含英文字符。每行表示以下内容之一：

| 行前缀 | 含义 | 格式 |
| -------- | ------ | ------ |
| `#` | 注释行 | `# 任意文本` |
| `P` | 项目名称 | `P 项目名称` |
| `T` | 普通任务（BasicTask） | `T ID 名称 工期` |
| `M` | 里程碑任务（MilestoneTask） | `M ID 名称 0` |
| `R` | 资源 | `R ID 名称 单位成本` |
| `D` | 依赖关系 | `D 前置任务ID 后置任务ID 类型 Lag` |
| `A` | 资源分配 | `A 任务ID 资源ID 数量` |

---

### 3. 字段说明（基于大作业 1.1~1.3 节定义）

| 字段 | 所属行 | 含义与详细说明 |
| :--- | :--- | :--- |
| **ID** | `T` / `M` / `R` | 任务或资源的唯一数字索引号（非负整数）。**任务 ID** 与 **资源 ID** 分别独立编号，允许数值相同（如任务 ID=1 与资源 ID=1 可共存），由内存中的 Project 容器分别维护两张映射表。 |
| **名称** | `T` / `M` / `R` | 任务或资源的描述性名称。**任务名称**在同一个 Project 中必须唯一，不可重复（参见 2.3.3 添加 Task 规则）。**资源名称**同样不可重复（参见 2.3.10 添加 Resource 规则）。 |
| **工期** | `T` / `M` | 完成任务所需的工作时间，单位为“天”，必须为**非负整数**。若工期 **> 0**，则为普通任务（BasicTask），可占用资源；若工期 **== 0**，则自动识别为里程碑任务（MilestoneTask），表示工程特定阶段的开始或结束，**不可占用任何资源**（参见 1.1 节）。 |
| **前置任务 ID** | `D` | 依赖关系中的**前序任务**（Predecessor）ID。该任务必须已存在于当前 Project 的任务列表中（参见 2.5.1 验证规则③）。 |
| **后置任务 ID** | `D` | 依赖关系中的**后续任务**（Successor）ID。该任务必须已存在于当前 Project 的任务列表中。 |
| **类型** | `D` | 任务间的时序逻辑类型，对应 1.2 节定义的四种依赖关系：<br><br>- **FS（完成-开始）**：前序任务完成后，后续任务才能开始。参照点为前置任务的**完成时间**。<br>- **SS（开始-开始）**：前序任务开始后，后续任务才能开始。参照点为前置任务的**开始时间**。<br>- **FF（完成-完成）**：前序任务完成后，后续任务才能完成。参照点为前置任务的**完成时间**。<br>- **SF（开始-完成）**：前序任务开始后，后续任务才能完成。参照点为前置任务的**开始时间**。 |
| **Lag** | `D` | 滞后/提前量（整数，可为正、负、零），表示**后置任务参照点**相对于**前置任务参照点**的时间偏移量（参见 1.2 节各依赖类型公式）：<br><br>- **FS**：`后置任务开始 = 前置任务完成 + Lag`<br>- **SS**：`后置任务开始 = 前置任务开始 + Lag`<br>- **FF**：`后置任务完成 = 前置任务完成 + Lag`<br>- **SF**：`后置任务完成 = 前置任务开始 + Lag`<br><br>**正数**表示滞后延迟（如等待混凝土凝固）；**负数**表示提前重叠（如前置工作未完全结束，后置工作即可开始或结束）。 |
| **单位成本** | `R` | 资源每单位时间（天）的租赁或使用成本，以数值表示（如 `100.0` 表示日薪 100 元）。一个任务可挂载多个资源，资源可在多个任务间**共享**（不限定资源总数，参见 1.3 节）。 |
| **数量** | `A` | 该任务同时占用该资源的数量（如 2 名开发人员、3 间会议室），必须为**正整数**。每个资源在该任务上的使用时长均**不超过该任务的总工期**（参见 1.1 节）。 |

---

### 4. 文件规则

- 所有 `P` 开头的行有且仅有 1 行，且位于文件最顶部区域（注释行之后）。
- 所有 `T` 和 `M` 开头的行必须连续出现，且均在所有 `D` 和 `A` 开头的行之前。
- 所有 `R` 开头的行必须连续出现，可以在 `D` 块之前或之后，但必须在 `A` 块之前。
- 所有 `D` 开头的行必须连续出现。
- 所有 `A` 开头的行必须连续出现，且位于文件末尾区域。

---

### 5. 示例

以下为 `sample.PPM` 文件内容示例：

```plaintext
# Sample Project for CPM Calculation
P ProjectDemo

T 1 Requirement 5
T 2 Design 3
T 3 Coding 7
T 4 Testing 4
T 5 Deployment 2
M 6 Acceptance 0

R 101 Architect 100.0
R 102 SeniorDev 80.0
R 103 JuniorDev 50.0
R 104 Tester 60.0
R 105 MeetingRoom 400.0

D 1 2 FS 0
D 2 3 FS 2
D 3 4 FS -1
D 4 5 FS 0
D 3 6 FS 0

A 1 101 1
A 1 105 3
A 2 102 2
A 3 102 2
A 3 103 1
A 4 104 2
A 5 103 1
```

#### 关键路径计算结果（基于该示例）

- 任务 1：ES=0，EF=5  
- 任务 2：ES=5+0=5，EF=8  
- 任务 3：ES=8+2=10，EF=17  
- 任务 4：ES=17+(-1)=16，EF=20  
- 任务 5：ES=20，EF=22  
- 任务 6（里程碑）：ES=17，EF=17  

**总工期 = 22 天**（任务 5 的 EF）。关键路径为：1 → 2 → 3 → 4 → 5。

---

### 6. 格式兼容性说明

为便于导入器的扩展设计，PPM 格式具备以下兼容性特征：

- **字段分隔符**：统一使用空格分隔，允许多个连续空格。
- **行首空白**：允许行首存在空格（解析时自动忽略）。
- **空行处理**：允许文件中的空行（解析时自动跳过）。
- **ID 唯一性**：任务 ID 和资源 ID 分别独立编号，允许任务 ID 与资源 ID 数值相同（例如，可以有任务 ID=1 和资源 ID=1，不冲突）。这是由 Project 容器分别维护两张映射表保证的。
