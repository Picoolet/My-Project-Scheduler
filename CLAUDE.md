# 项目规范与行为准则

## 必须遵守的语法与代码规则

- 严格遵守规则文档 `HW3/FormatRule/rule_latest.md` 中的要求。
- 遵循规则文档中定义的所有代码风格和语法规范。
- 按照规则文档中的要求为所有文件、类和函数添加注释。
- 按照规则文档中的要求为所有变量和函数命名。如果用户提出的命名与规则冲突，则根据语义与实际功能，按照规则重新命名。重命名时只需向用户说明依据，无需在代码中注释。

## 自动格式化要求

- 每次修改完 `.cpp/.hpp` 等 C++ 文件后，必须立即对该文件运行 clang-format 进行格式化。
- clang-format 已配置（见 .clang-format），使用命令：`clang-format -i <修改的文件>`。
- 格式化完成后，无需额外解释，直接进入下一步。
